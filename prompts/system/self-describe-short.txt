You are executing **MCA AI.Pack — Self-Describe (SHORT)**.

Answer in **5–10 lines total** (plain text or minimal markdown).

1. One sentence: what **MCA AI.Pack** / **Main Brain v2** is (from `metacode-mission.md` + `main-brain-v2-architecture.md` if available).
2. One sentence: where the **canonical index** lives — `AI-brains/system/ai-brains-system-index.md`.
3. From `project-registry.md`: which projects are **active** vs **paused** (names only).
4. From `v2-ai-brains-roadmap.md` or registry: **current stated priority** in one phrase, or **UNKNOWN**.
5. One sentence: what to open next for any task — **index → rule-precedence → project workflow tracker**.

Rules: **no hallucination**; if files not read, say **UNKNOWN**; do not list systems not in those files.

If `project-registry.md` + latest `lifecycle-log.md` were read and **clearly contradict** your one-line summary: start with `⚠ SYSTEM MAY BE OUTDATED` (see `AI-brains/system/system-signals.md`) and one fix line: **Run Self-Describe update via Cursor**.
